# Hyderabad Hangama Club — environment variable index
#
# Copy platform-specific examples:
#   apps/api/.env              ← local API (see apps/api/.env.example)
#   apps/web/.env.local        ← local web (see apps/web/.env.example)
#
# Production templates (do not commit secrets):
#   render-env-example.txt     ← Render (FastAPI)
#   vercel-env-example.txt     ← Vercel (Next.js)
#   supabase-env-example.txt   ← Supabase dashboard
#
# Deployment guide: docs/DEPLOYMENT_READY.md
